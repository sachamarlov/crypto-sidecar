# 000 — Tauri frontend — task breakdown

- [ ] **T-000fe.01** — Setup OpenAPI codegen pipeline:
      `scripts/export_openapi.py` (Python), `pnpm openapi:generate`,
      `src/api/schema.d.ts` committed (regenerated by CI).
- [ ] **T-000fe.02** — `src/api/client.ts` with token + session
      interceptors; `useSidecarConnection()` hook reading
      `invoke('get_sidecar_connection')` at boot.
- [ ] **T-000fe.03** — TanStack Router scaffolding (`routes/`):
      `__root.tsx` (Providers + Toaster), `index.tsx` redirect,
      `lock.tsx`, `dashboard.tsx` parent + nested modal routes.
- [ ] **T-000fe.04** — Lock state: Jotai atoms (`sessionId`,
      `expiresAt`, `isUnlocked`), `useAutoLock()` hook, AuthGuard
      component.
- [ ] **T-000fe.05** — `<PasswordField>` shared component
      (no-echo Input + zxcvbn live strength bar).
- [ ] **T-000fe.06** — `LockScreen` (admin password unlock + init
      vault flow).
- [ ] **T-000fe.07** — `DashboardScreen` (vault items DataTable +
      action menu + floating CTA).
- [ ] **T-000fe.08** — `EncryptModal` (file picker + password +
      KDF select + progress).
- [ ] **T-000fe.09** — `DecryptModal` (anti-oracle: constant toast
      text on every post-KDF failure).
- [ ] **T-000fe.10** — `ShareModal` (2-step: preview fingerprint
      then commit).
- [ ] **T-000fe.11** — `AcceptModal` (token import + sender select + recipient password).
- [ ] **T-000fe.12** — `HistoryModal` (DataTable + filters +
      verify-chain button).
- [ ] **T-000fe.13** — `UsersModal` (create / list / delete /
      change-password).
- [ ] **T-000fe.14** — `SettingsModal` (read-only config dump +
      doctor diagnostics).
- [ ] **T-000fe.15** — i18n FR + EN (`react-i18next`,
      `i18next-browser-languagedetector`).
- [ ] **T-000fe.16** — WCAG 2.2 AA: focus rings, ARIA, reduced-
      motion, skip-link; axe-playwright passes on every screen.
- [ ] **T-000fe.17** — Vitest unit tests on hooks + stores +
      components critiques (≥ 80 % global, ≥ 95 % on `api/` +
      `hooks/`).
- [ ] **T-000fe.18** — Playwright E2E flows: lock+init,
      encrypt/decrypt round-trip, share+accept, anti-oracle,
      auto-lock, audit history, secure-delete, users CRUD.
- [ ] **T-000fe.19** — Biome strict + tsconfig strict + Tauri
      capabilities allowlist tightening.
- [ ] **T-000fe.20** — `pnpm-lock.yaml` committed; CI frontend
      job activated (lint / typecheck / vitest).

## Definition of Done

| Gate                                           | Status |
| ---------------------------------------------- | ------ |
| 9 screens render at parity with CLI / TUI      | TBD    |
| Anti-oracle byte-identical on Decrypt + Accept | TBD    |
| FR + EN strings cover 100 % of UI              | TBD    |
| WCAG 2.2 AA on every screen via axe-playwright | TBD    |
| Vitest coverage ≥ 80 % overall                 | TBD    |
| 8 Playwright E2E flows pass                    | TBD    |
| `pnpm lint` (biome) + `pnpm typecheck` clean   | TBD    |
| CI Frontend job green                          | TBD    |
