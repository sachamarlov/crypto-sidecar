"""Placeholder per the academic brief (CDC) which prescribes a ``tests/``
directory inside the package. The authoritative test suite lives at
``tests/`` in the repository root (modern Python convention) — keep this
package empty to satisfy the CDC layout while avoiding test duplication.
"""
