{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for GuardiaBox. Permissions are progressively narrowed as the UI grows; the defaults cover window chrome + standard plugin families only.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "notification:default",
    "os:default",
    "shell:default",
    "clipboard-manager:default",
    "store:default",
    "window-state:default",
    "global-shortcut:default"
  ]
}
