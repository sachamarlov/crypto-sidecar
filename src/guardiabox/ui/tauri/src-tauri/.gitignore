# Generated by Cargo. Cargo.lock is intentionally tracked because
# this crate is a [[bin]] application: per the official Rust
# guidelines, application binaries commit their lockfile so every
# CI run + every contributor links the same transitive deps.
# Audit D P0-3.
/target

# Tauri generated assets
/gen

# Bundled sidecar binary copied during CI
/binaries
